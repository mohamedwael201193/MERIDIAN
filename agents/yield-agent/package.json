{
  "name": "@meridian/yield-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@meridian/agents-shared": "workspace:*"
  },
  "devDependencies": {
    "vitest": "=3.2.4"
  }
}
